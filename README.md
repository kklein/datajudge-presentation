Generate presentation as html file with hot reload:

```console
% marp -w slides.md -o slides.html --html
```

Generate presentation as pdf file:
```console
$ marp slides.md -o slides.pdf --html --allow-local-files
```
